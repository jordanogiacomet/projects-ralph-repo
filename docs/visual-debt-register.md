# Visual Debt Register

Generated by `US-052` on `2026-03-11`.

## Audit Scope

- Breakpoints: `desktop 1440x1800`, `tablet 834x1194`, `mobile 390x844`
- Primary public routes audited in default state:
  - `/`
  - `/sobre`
  - `/solucoes`
  - `/solucoes/avaliacao-patrimonial`
  - `/solucoes/controle-patrimonial`
  - `/solucoes/consultoria-e-tecnologia`
  - `/clientes`
  - `/conteudos`
  - `/conteudos/e-book-identificacao-patrimonial`
  - `/news`
  - `/contato`
  - `/contato/cotacao`
  - `/contato/representante`
- Additional state coverage:
  - `hover/open/active`: desktop mega menu, desktop search overlay, mobile menu, active shell states
  - `empty`: global search zero-results state, News archive zero-results search state
  - `loading`: global search async loading state
  - `error`: representative missing-route 404 at `/news/nao-existe`

## Audit Notes

- No default-state horizontal overflow was detected on the audited primary routes at the three breakpoints above.
- The default contact flows were reviewed visually, but inline form validation and success/error messaging did not produce stable headless captures in this pass; `US-056` should still manually confirm those interactive states after shell fixes land.
- No solution-family issue severe enough to log separately emerged outside the shared shell/floating problems below; `US-057` should re-check that family once `US-053` is complete.

## Debt Items

| ID | Target Story | Route(s) | Breakpoint(s) | Component | State | Severity | Problem | Expected |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `VD-001` | `US-053` | All primary public routes | `tablet`, `mobile` | Cookie consent | `default` | `high` | The consent panel occupies a large part of the first viewport and blocks hero copy and CTAs before the user can assess the page. | Consent should remain readable and actionable without covering the main headline or first CTA cluster. |
| `VD-002` | `US-053` | All primary public routes | `mobile`, `tablet` | WhatsApp float | `default` | `medium` | The floating WhatsApp chip sits on top of lower content, footer actions, and the consent surface on smaller screens, creating stacked floating chrome. | Floating contact affordances should clear the content edge and avoid collisions with consent or footer controls. |
| `VD-003` | `US-053` | Global shell | `mobile` | Mobile menu | `open` | `medium` | The open menu gives the first viewport to an oversized search tile and decorative panels, while the actual navigation groups start below the fold. | The first mobile-menu viewport should prioritize route access and keep the primary navigation groups immediately visible. |
| `VD-004` | `US-053` | Global shell | `desktop` | Search overlay | `loading` | `low` | The loading state leaves most of the result pane empty and places weak status feedback away from the search field, so the overlay feels stalled. | Loading feedback should stay visually anchored to the query field and preserve stronger spatial guidance while results resolve. |
| `VD-005` | `US-054` | `/` | `desktop`, `tablet`, `mobile` | Homepage hero media | `default` | `medium` | The hero requests `/images/home-hero-placeholder.jpg`, but that fallback asset is missing, so the route drops to a flatter gradient-only treatment. | The homepage hero needs a real fallback asset or an intentional non-broken fallback treatment that does not 404. |
| `VD-006` | `US-054` | `/` | `mobile` | Homepage hero | `default` | `medium` | The mobile headline stack and vertical spacing leave the secondary CTA cut off below the first viewport even after consent is dismissed. | Both hero CTAs should read as a deliberate pair within the first mobile viewport. |
| `VD-007` | `US-054` | `/sobre` | `desktop`, `tablet`, `mobile` | Institutional slider background | `default` | `medium` | The institutional slider falls back to `/images/sobre-slide-placeholder.jpg`, but that asset is missing, so the storytelling surface loses intended image depth. | The `/sobre` slider needs a real fallback asset or an intentional non-broken fallback treatment that preserves the section’s visual weight. |
| `VD-008` | `US-055` | `/news/nao-existe` | `desktop`, `tablet`, `mobile` | Error / not-found template | `error` | `high` | The route falls back to the generic English Next.js 404 view with large empty whitespace and no Apollo-specific recovery path. | Missing-route states should inherit Apollo language, visual system, and recovery actions. |
| `VD-009` | `US-055` | `/news` | `tablet` | News archive empty state | `empty` | `low` | When filters return zero items, the above-the-fold view emphasizes counts and the search field while the explanatory empty-state message sits lower in the layout. | The recovery copy for zero-results states should sit closer to the filter/search controls so the next action is obvious immediately. |
