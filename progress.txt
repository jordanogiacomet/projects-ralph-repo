# Ralph Progress Log
Started: Mon Mar  9 09:19:04 PDT 2026
---

## Codebase Patterns
- Payload CMS 3.x is integrated into Next.js (not standalone) — config at `src/payload.config.ts`
- Frontend routes live in `src/app/(frontend)/`, Payload admin/API in `src/app/(payload)/`
- Use `@payload-config` path alias to import payload config
- Use `@/*` path alias for src imports
- Tailwind design tokens: accent (#0056A6), bg-dark-section (#1A1A2E), cta-green (#28A745)
- Collections go in `src/collections/`, globals in `src/globals/`, blocks in `src/blocks/`
- Use `--legacy-peer-deps` for npm install due to Payload/Next.js peer dep conflicts
- Next.js version must match Payload peer requirements (currently 15.4.x)
- Access control helpers at `src/access/index.ts` — use `isAdmin`, `isAdminOrEditor`, `isLoggedIn`, `anyone`
- Slug auto-generation hook at `src/hooks/formatSlug.ts` — use `formatSlugHook` in field `hooks.beforeValidate`
- Collection slugs use kebab-case (e.g., `solucao-categories`, `cliente-segmentos`)
- Block definitions live in `src/blocks/`, exported via `src/blocks/index.ts`
- BlockRenderer component at `src/components/BlockRenderer.tsx` maps blockType to React components
- Import all blocks from `@/blocks` barrel export when registering in collections
- Server/client split for interactive layout components: FooServer.tsx (async, fetches data) + Foo.tsx ('use client', handles state/events)
- Payload upload relations may be string IDs or objects — always check `typeof field === 'object'` before accessing `.url`
- Social icons are inline SVGs in SocialLinks.tsx — no icon library needed
- Use framer-motion AnimatePresence for enter/exit animations on conditional renders

---

## 2026-03-09 - US-001
- Setup Next.js 15.4 + Payload CMS 3.79 + PostgreSQL project
- Tailwind CSS 3.x with PRD color tokens configured
- TypeScript strict mode, ESLint (next/core-web-vitals), Prettier
- Folder structure created per PRD specification
- Users, Media, Pages collections; seed script placeholder
- Files changed: package.json, tsconfig.json, next.config.mjs, tailwind.config.ts, postcss.config.mjs, eslint.config.mjs, .prettierrc, .gitignore, .env.example, src/payload.config.ts, src/collections/*, src/lib/*, src/app/*, src/seed.mjs
- **Learnings for future iterations:**
  - `create-payload-app` CLI doesn't work without TTY — build project manually instead
  - Payload 3.79 requires Next.js >=15.2.9 <15.3.0 || >=15.3.9 <15.4.0 || >=15.4.11 <15.5.0
  - The `@payloadcms/storage-local` package doesn't exist — just use Media collection `upload.staticDir`
  - Payload admin layout needs `importMap.js` in admin folder and `ServerFunctionClient` in layout
  - `sharp` must be dynamically imported in payload config: `sharp: (await import('sharp')).default`
---

## 2026-03-09 - US-002
- Created all Payload CMS collections: Posts, Solucoes, SolucaoCategories, Clientes, ClienteSegmentos, Conteudos, FormSubmissions, NewsletterSubscribers
- Updated existing collections: Users (name, role fields + access control), Media (caption field), Pages (hero fields, layout blocks, parent, meta, showContactForm)
- Created all globals: Header (logo, navItems nested, ctaButton, socialLinks), Footer (unidades, copyrightText, socialLinks), SiteSettings (whatsapp, cookie, defaultMeta, analytics, maintenanceMode)
- Created slug auto-generation hook at src/hooks/formatSlug.ts (normalizes accented chars, generates URL-safe slugs)
- Created access control helpers at src/access/index.ts (isAdmin, isAdminOrEditor, isLoggedIn, isAdminOrSelf, anyone)
- Updated payload.config.ts to register all 11 collections and 3 globals
- Files changed: src/payload.config.ts, src/collections/*, src/globals/*, src/hooks/formatSlug.ts, src/access/index.ts
- **Learnings for future iterations:**
  - Access control: use `@/access` helpers — `isAdmin`, `isAdminOrEditor`, `isLoggedIn`, `anyone`
  - Slug hook: use `formatSlugHook` from `@/hooks/formatSlug` on slug fields with `hooks.beforeValidate`
  - Collection slugs use kebab-case (e.g., `solucao-categories`, `cliente-segmentos`, `form-submissions`)
  - Payload `hasMany: true` on text fields creates tag-like arrays
  - Payload blocks field on Pages has empty blocks array — will be populated when blocks are created in US-003
  - FormSubmissions uses `json` type field for flexible form data storage
---

## 2026-03-09 - US-003
- Created 11 Payload block definitions in src/blocks/: HeroBlock, RichContentBlock, ServiceCardsBlock, CTABlock, AccordionBlock, StatsBlock, ContactFormBlock, ImageTextBlock, ClientLogosBlock, VideoBlock, DownloadBlock
- Created barrel export at src/blocks/index.ts
- Created BlockRenderer component at src/components/BlockRenderer.tsx with placeholder renderers for each block type
- Registered all 11 blocks in Pages collection layout field
- Files changed: src/blocks/* (12 new files), src/components/BlockRenderer.tsx, src/collections/Pages.ts
- **Learnings for future iterations:**
  - Block slugs use camelCase (e.g., `heroBlock`, `richContentBlock`) — Payload convention
  - BlockRenderer uses `data-block-type` attribute for easy debugging/styling
  - Placeholder block components use `Record<string, unknown>` props — will need proper typing when implementing frontend rendering
  - `void props` pattern used to suppress unused variable warnings in placeholder components
---

## 2026-03-09 - US-004
- Created Navbar with fixed positioning, scroll-based transparency/solid toggle, mega menu, mobile hamburger, search overlay, and social links bar
- Components: Navbar.tsx (main client component), NavbarServer.tsx (server wrapper fetching Header global), MegaMenu.tsx (animated 3-column dropdown), MobileMenu.tsx (slide-in with accordion sub-menus and focus trap), SocialLinks.tsx (reusable social icon links), SearchOverlay.tsx (fullscreen search with framer-motion)
- Updated frontend layout.tsx to include NavbarServer
- Files changed: src/components/Navbar.tsx, src/components/NavbarServer.tsx, src/components/MegaMenu.tsx, src/components/MobileMenu.tsx, src/components/SocialLinks.tsx, src/components/SearchOverlay.tsx, src/app/(frontend)/layout.tsx
- **Learnings for future iterations:**
  - Server/client component split: NavbarServer.tsx (server) fetches Payload globals, Navbar.tsx (client) handles interactivity — use this pattern for other layout components
  - Payload global fields may return string IDs or full objects for `upload` relations — always check `typeof field === 'object'` before accessing `.url`
  - `isHeroPage` prop controls transparent navbar behavior — pass from page-level to NavbarServer when page has fullscreen hero
  - Framer Motion AnimatePresence requires immediate children to have `key` props for exit animations
  - Focus trap in MobileMenu uses querySelectorAll for focusable elements and Tab key handling
  - Social link SVG icons are inline (no icon library dependency) — platform icons defined in SocialLinks.tsx
---
## 2026-03-09 10:38 UTC - US-005
- Validated and completed global footer experience wired to Payload Globals: contact form area + illustration, unidades list, social links, copyright with dynamic year, scroll-to-top, cookie consent, and WhatsApp floating CTA in frontend layout
- Files changed: prd.json, progress.txt
- Quality checks run and results: `npm run lint` passed; `npm run build` passed (with runtime log warning for unavailable PostgreSQL in this sandbox: `connect EPERM 127.0.0.1:5432`); `npm run typecheck` not available (missing script); `npm run test` not available (missing script)
- Browser/manual verification status: Browser tooling unavailable in this environment; manual UI verification still required (desktop + mobile)
- **Learnings for future iterations:**
  - `next build` can complete even when Payload cannot reach Postgres during static generation in this sandbox; treat DB connectivity logs separately from build exit status
  - Footer/social/whatsapp/cookie data flow depends on `footer` and `site-settings` globals, so those globals must be seeded/configured before visual QA
---
## 2026-03-09 10:46 UTC - US-006
- Implemented homepage with 100vh hero (dark overlay + animated scroll indicator), subtle parallax, 3 animated service cards, partner CTA section, and "Quem é a Apollo" section with optional embedded video/image and `/sobre` link
- Wired homepage content to Payload `pages` document (`slug: home`) using `hero*` fields plus `layout` blocks (`serviceCardsBlock`, `ctaBlock`, `videoBlock`, `imageTextBlock`) with safe defaults when CMS/DB is unavailable
- Files changed: src/app/(frontend)/page.tsx, src/components/HomePage.tsx, prd.json, progress.txt
- Quality checks run and results: `npm run lint` passed; `npm run build` passed (with sandbox DB connectivity log during static generation: `connect EPERM 127.0.0.1:5432`); `npm run typecheck` not available (validated with `npx tsc --noEmit`, passed); `npm run test` not available (missing script)
- Browser/manual verification status: Browser tooling unavailable in this environment; manual UI verification still required (desktop + mobile)
- **Learnings for future iterations:**
  - Homepage can be CMS-driven without schema changes by composing existing `Pages` `hero*` fields with `layout` blocks and deterministic block selection
  - When `next build` exits with worker code `1` but no visible error, running `npx tsc --noEmit` surfaces strict TypeScript issues immediately
  - Keep frontend resilience by wrapping Payload fetches in `try/catch` and supplying fallback content for static builds without DB access
---
